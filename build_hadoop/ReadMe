This script is to build the hadoop source code on a bare metal
For now, do not run the script without root priveleges


Install an set java
Install and setup protocol-buffers2.5.0
Install and setup maven 3.3.9
Run a clean install to install dependencies
Run a build
Untar file to the usr/local/hadoop1
Install ssl and other dependencies
